services:
  dashboard:
    build:
      context: .
    container_name: sg-foodprice-tracker-dashboard
    env_file:
      - .env
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    command: streamlit run dashboard/Home.py --server.address=0.0.0.0 --server.port=8501
